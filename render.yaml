services:
  - type: web
    name: carevault-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm run start
    plan: free
    envVars:
      - key: PORT
        value: '3000'
